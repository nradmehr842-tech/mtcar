# MTcar v25.5 — دسترسی آفلاین رابط کاربری

این نسخه همان شاخه اصلی برنامه است و ورود سروری عادی بدون تغییر باقی مانده است.

## ورود آفلاین محلی
- نام کاربری: `mtcar`
- رمز عبور: `mtcar1405`

این ورود فقط یک نشست محلی داخل APK ایجاد می‌کند تا رابط کاربری قبل از آماده شدن VPS/API قابل مشاهده باشد. توکن محلی توسط Backend معتبر نیست و به داده‌های ابری، اشتراک واقعی، دستگاه‌های سرور یا فرمان‌های سروری دسترسی نمی‌دهد.

پس از آماده شدن سرور، برای استفاده واقعی از حساب سروری خارج شوید و با حساب واقعی وارد شوید. برای انتشار عمومی نهایی، حذف دسترسی آفلاین ثابت توصیه می‌شود.
