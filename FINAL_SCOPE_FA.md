# محدوده نهایی پروژه MTcar — v23 Main Source

این بسته، سورس اصلی یکپارچه MTcar است که باید مبنای ادامه توسعه قرار بگیرد.

## معماری

- `mobile/` : اپلیکیشن Flutter
- `server/` : بک‌اند Node.js / API
- `preview/` : پیش‌نمایش تحت وب
- `admin_preview/` : پیش‌نمایش پنل مدیریت
- `website/` : لندینگ/وب‌سایت
- `ui_reference_locked/` : تصاویر مرجع نهایی رابط کاربری

## تصمیم‌های نهایی محصول

### 1) ردیاب‌ها و مدل‌ها
- فعلاً اپ کاربر فقط با مدل نمونه `MT120` جلو می‌رود.
- انتخاب مدل از فرایند ورود/ثبت‌نام حذف شده است.
- ساختار سرور برای مدیریت مدل‌های جدید حفظ می‌شود.
- مدیر باید بعداً بتواند از پنل مدیریت، مدل جدید اضافه کند:
  - نام مدل
  - برند
  - پورت
  - پروتکل
  - تنظیمات لازم
  - توضیحات داخلی
- اپ اصلی باید این مدل‌ها را بعداً از سرور بخواند.

### 2) حساب کاربری و اشتراک
- ورود با شماره موبایل و رمز حساب
- ثبت‌نام با OTP
- تغییر شماره موبایل و تغییر رمز از مسیر امن
- یک ماه اول پس از اولین آنلاین‌شدن واقعی دستگاه رایگان است
- پس از پایان اعتبار، بنر تمدید در صفحه اصلی نمایش داده می‌شود
- تمدید اشتراک از بک‌اند و درگاه سمت سرور انجام می‌شود

### 3) سیم‌کارت و شارژ
- نمایش وضعیت سیم‌کارت
- نمایش مانده شارژ اصلی و اینترنت فقط در صورت دریافت معتبر از سرویس سمت سرور/اپراتور
- خرید شارژ و بسته اینترنت باید از طریق Provider سمت سرور انجام شود
- UI این قابلیت‌ها باید وجود داشته باشد، حتی اگر Provider واقعی بعداً متصل شود

### 4) کنترل‌ها و امنیت
- آژیر خودرو
- محافظت
- شنود آنلاین
- یافتن خودرو
- قطع موتور
- هشدارها و رویدادها
- نقشه زنده
- پخش مسیر

### 5) UI نهایی
- تنها مرجع مجاز برای ظاهر اپ، تصاویر موجود در `ui_reference_locked/` است.
- هرگونه اختلاف میان UI فعلی کد و این تصاویر باید در ادامه توسعه به نفع تصاویر مرجع اصلاح شود.

## وضعیت این بسته

این بسته برای ادامه توسعه «نسخه اصلی» آماده شده است:
- ساختار بک‌اند موجود است
- ساختار اپ موبایل موجود است
- مرجع نهایی UI قفل شده است
- تصمیم‌های محصول و فنی نهایی ثبت شده‌اند

مرحله بعدی: همسان‌سازی کامل همه صفحات Flutter با تصاویر مرجع و اتصال نهایی به APIهای واقعی.

## احراز هویت بدون پیامک — تصمیم v25

این بخش جایگزین تصمیم‌های قدیمی OTP در همین سند است:

- ثبت‌نام فقط با نام کاربری، رمز عبور و تکرار رمز عبور انجام می‌شود.
- ورود فقط با نام کاربری و رمز عبور انجام می‌شود.
- سرویس پیامک برای ثبت‌نام، ورود یا تغییر رمز لازم نیست.
- بازیابی خودکار رمز فعلاً غیرفعال است؛ تغییر رمز در حساب فعال با رمز فعلی انجام می‌شود.
- SMS Backup مربوط به ردیاب اختیاری است و فقط در صورت فعال‌سازی توسط کاربر مجوز SMS درخواست می‌کند.
